# Olivia — Windows-focused Python voice assistant.
# Install: pip install -r requirements.txt

# Offline core voice I/O
faster-whisper==1.2.1   # local Whisper STT (CTranslate2) — offline after 1st model download
sounddevice==0.5.5      # mic capture into a numpy array
numpy==2.2.6            # audio buffer
pyttsx3==2.99           # offline TTS (SAPI5 on Windows)

# Offline skills
pyautogui==0.9.54       # desktop automation, screenshots
pyperclip==1.11.0        # clipboard read-aloud
psutil==7.2.2           # CPU/RAM/disk/battery monitoring
pyjokes==0.8.3          # offline jokes

# Online skills (search/wikipedia, weather, news, translate) — need internet.
# Disabled when OLIVIA_OFFLINE_ONLY=1.
pywhatkit==5.4          # play on YouTube
wikipedia==1.4.0        # "tell me about" / "who is"
requests==2.34.2        # news, public IP, dad jokes
googletrans==4.0.2    # translation
pyowm==3.5.0            # weather (OpenWeatherMap)
speedtest-cli==2.1.3    # internet speed test

# Windows-only (skip on Linux/macOS)
winshell==0.6 ; sys_platform == "win32"
pywin32==312 ; sys_platform == "win32"
